# Benchmarks

This project includes a local benchmark script:

```bash
npm run bench
```

It currently measures:

- `.glass` parsing
- config normalization
- CSS compilation

Script location:

- `scripts/benchmark.mjs`

## Notes

- Benchmark numbers are machine-dependent.
- Use this script comparatively (before/after changes), not as absolute cross-machine proof.
